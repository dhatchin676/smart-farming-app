# Deploy from REPO ROOT — so both frontend/ and backend/ are available
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "cd backend && node server.js"